# The one and only
export EDITOR='vim'

# tmux needs screen-256 color for ncurses to not get corrupted
export TERM=screen-256color

# extra PATH for other languages
export PATH=$PATH:$HOME/.cabal/bin:$HOME/.rbenv/bin
